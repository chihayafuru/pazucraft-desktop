{
  "name": "typescript_electron_app",
  "version": "0.1.0",
  "description": "Electron App with TypeScript",
  "main": "app/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "$(npm bin)/electron .",
    "build": "$(npm bin)/gulp build",
    "clean": "$(npm bin)/gulp clean"
  },
  "author": "Takehiko YOSHIDA",
  "license": "MIT",
  "devDependencies": {
    "del": "^2.2.2",
    "electron": "^1.4.12",
    "gulp": "^3.9.1",
    "gulp-typescript": "^3.1.3",
    "typescript": "^2.1.4"
  },
  "dependencies": {
    "@types/electron": "^1.4.29"
  }
}
